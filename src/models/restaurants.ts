export interface Resto{
    id: string,
    naam: string,
    teller: number
    isChecked: boolean,
}

export interface PostResto{
    naam: string,
}

export interface DeleteResto{
    id: string,
    naam: string,
}